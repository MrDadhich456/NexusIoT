# Stream Processor dependencies (minimal — keeps Docker image small)
confluent-kafka==2.6.1
pydantic==2.9.0
structlog==24.4.0
prometheus-client==0.21.0

# ML / XAI (SHAP explainer - Step 6)
# IsolationForest for unsupervised anomaly model, SHAP for feature attribution
scikit-learn==1.5.2
shap==0.46.0
numpy==2.1.3
pandas==2.2.3

# Database (TimescaleDB writer - Step 7)
# psycopg v3: modern PostgreSQL adapter with native connection pooling
# [binary] variant includes pre-compiled C extension for faster installs
psycopg[binary]==3.2.4
psycopg-pool==3.2.4
